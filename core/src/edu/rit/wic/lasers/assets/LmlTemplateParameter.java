package edu.rit.wic.lasers.assets;

import com.badlogic.gdx.assets.AssetLoaderParameters;

/**
 * Required, and semantically useful, for {@link LmlTemplateLoader}. Ideally this would be used in the future if we
 * need to parameterize the creation of an {@link LmlTemplate} using the loader.
 *
 * @author Matthew Crocco
 */
public class LmlTemplateParameter extends AssetLoaderParameters<LmlTemplate> {

}
